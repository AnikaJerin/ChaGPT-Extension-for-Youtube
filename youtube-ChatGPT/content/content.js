// This script is injected into the YouTube video page
// Currently we do not need much here
// But we could, for example, extract videoId if needed
// Currently we extract it directly from tab.url in popup.js
